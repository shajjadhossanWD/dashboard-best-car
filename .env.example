# Fraction of mock API calls that should fail (0 - 1).
# Handy for reviewing the per-widget error + retry states.
VITE_API_FAILURE_RATE=0

# Base URL of the Python AI service (../ai_service).
VITE_AI_URL=http://localhost:8000
